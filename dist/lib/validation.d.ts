import { ValueSchema, ValueValidationFn } from 'simplity-types';
export declare const DEFAULT_MAX_CHARS = 1000;
export declare const DEFAULT_DAYS_RANGE = 365000;
export declare const DEFAULT_MAX_NUMBER: number;
export declare const DEFAULT_NBR_DECIMALS = 2;
/**
 * creates a function that can be used to validate a value against the supplied value-schema
 * @param schema schema json. Typically entered as met-date by designers, or generated by utilities
 * @returns validation function
 */
export declare function createValidationFn(schema: ValueSchema): ValueValidationFn;
